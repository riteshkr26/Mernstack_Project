<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ritesh Kumar | Full Stack Developer & Pro Gamer</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #ff4655; 
            --primary-glow: rgba(255, 70, 85, 0.5);
            --bg: #0f172a;
            --card-bg: rgba(30, 41, 59, 0.7);
            --text: #f8fafc;
            --text-dim: #94a3b8;
            --glass: rgba(255, 255, 255, 0.05);
            --nav-bg: rgba(15, 23, 42, 0.95);
        }

        [data-theme="light"] {
            --bg: #f1f5f9;
            --card-bg: rgba(255, 255, 255, 0.9);
            --text: #0f172a;
            --text-dim: #475569;
            --glass: rgba(0, 0, 0, 0.05);
            --nav-bg: rgba(241, 245, 249, 0.95);
        }

        html { scroll-behavior: smooth; }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg);
            color: var(--text);
            margin: 0;
            line-height: 1.6;
            transition: all 0.4s ease;
        }

        /* --- NAVIGATION --- */
        nav {
            position: sticky;
            top: 0;
            background: var(--nav-bg);
            backdrop-filter: blur(12px);
            padding: 15px 0;
            z-index: 1000;
            border-bottom: 1px solid var(--glass);
            text-align: center;
        }

        nav a {
            color: var(--text);
            text-decoration: none;
            margin: 0 15px;
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            transition: 0.3s;
        }

        nav a:hover { color: var(--primary); text-shadow: 0 0 10px var(--primary-glow); }

        .theme-toggle {
            cursor: pointer;
            background: var(--primary);
            border: none;
            color: white;
            padding: 8px 18px;
            border-radius: 50px;
            font-weight: bold;
            margin-left: 20px;
            transition: 0.3s;
        }

        .container { max-width: 1100px; margin: 0 auto; padding: 40px 20px; }

        /* --- HERO --- */
        header { text-align: center; margin-bottom: 80px; }
        .hero-title {
            font-family: 'Orbitron', sans-serif;
            font-size: clamp(2.5rem, 8vw, 4rem);
            color: var(--primary);
            letter-spacing: 5px;
            text-shadow: 0 0 25px var(--primary-glow);
        }

        /* --- PROFILE SECTION --- */
        .profile-section {
            display: flex;
            gap: 50px;
            background: var(--card-bg);
            padding: 50px;
            border-radius: 30px;
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass);
            flex-wrap: wrap;
            justify-content: center;
        }

        .profile-img img {
            width: 280px;
            height: 320px;
            object-fit: cover;
            border-radius: 20px;
            border: 2px solid var(--primary);
            transition: 0.5s;
        }

        .profile-img img:hover {
            transform: scale(1.05) rotate(2deg);
            box-shadow: 0 0 30px var(--primary-glow);
        }

        .about-text ul { list-style: none; padding: 0; }
        .about-text li { margin-bottom: 10px; font-size: 1.1rem; }
        .about-text b { color: var(--primary); }

        /* --- SKILLS --- */
        .skill-bar { background: var(--glass); height: 10px; border-radius: 10px; margin: 10px 0 20px; overflow: hidden; }
        .skill-per { background: var(--primary); height: 100%; width: 0; transition: 2s ease-in-out; }

        /* --- SOCIAL LINKS --- */
        .social-links { display: flex; gap: 15px; margin-top: 25px; }
        .social-links a {
            color: var(--text);
            text-decoration: none;
            border: 1px solid var(--primary);
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: bold;
            transition: 0.4s;
        }
        .social-links a:hover { background: var(--primary); color: white; box-shadow: 0 0 15px var(--primary-glow); }

        /* --- EDUCATION TABLE --- */
        .table-container { overflow-x: auto; border-radius: 15px; background: var(--glass); padding: 20px; margin-top: 20px; }
        table { width: 100%; border-collapse: collapse; text-align: left; }
        th, td { padding: 15px; border-bottom: 1px solid var(--glass); }
        th { color: var(--primary); font-family: 'Orbitron'; }

        /* --- PROJECT GRID --- */
        .project-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 25px; margin-top: 30px; }
        .project-card { background: var(--glass); padding: 30px; border-radius: 20px; border: 1px solid var(--glass); transition: 0.4s; }
        .project-card:hover { transform: translateY(-10px); border-color: var(--primary); background: var(--card-bg); }

        /* --- CONTACT --- */
        .contact-box { background: var(--card-bg); padding: 40px; border-radius: 25px; border: 1px solid var(--glass); }
        input, textarea {
            width: 100%; padding: 15px; margin-bottom: 15px; background: var(--bg);
            border: 1px solid var(--glass); color: var(--text); border-radius: 10px; box-sizing: border-box;
        }

        @media (max-width: 768px) { .profile-section { flex-direction: column; text-align: center; } .social-links { justify-content: center; } }
    </style>
</head>
<body onload="animateSkills()">

<nav>
    <a href="#about">About</a>
    <a href="#education">Education</a>
    <a href="#projects">Projects</a>
    <a href="#contact">Contact</a>
    <button class="theme-toggle" onclick="toggleTheme()">🌓 Theme</button>
</nav>

<div class="container">
    <header>
        <h1 class="hero-title">Ritesh Kumar</h1>
        <p style="letter-spacing: 3px; font-weight: bold; color: var(--primary);">FULL STACK DEVELOPER • ESPORTS ATHLETE</p>
    </header>

    <section id="about" class="profile-section">
        <div class="profile-img">
            <img src="Profile.jpeg" alt="Ritesh Kumar">
        </div>
        <div class="about-text">
            <h2 style="font-family: 'Orbitron';">About Me</h2>
            <ul>
                <li><b>NAME:</b> Ritesh Kumar</li>
                <li><b>AGE:</b> 22 Years</li>
                <li><b>LOCATION:</b> India</li>
                <li><b>SKILLS:</b> JavaScript, React, Node.js</li>
            </ul>
            
            <small>Frontend Development</small>
            <div class="skill-bar"><div class="skill-per" id="skill1" data-width="90%"></div></div>
            <small>Backend (Node.js)</small>
            <div class="skill-bar"><div class="skill-per" id="skill2" data-width="75%"></div></div>

            <div class="social-links">
                <a href="https://www.instagram.com/ritesh_sharma026/" target="_blank">Instagram</a>
                <a href="https://www.linkedin.com/in/ritesh-kumar-7623b9282/" target="_blank">LinkedIn</a>
                <a href="https://github.com/riteshkr26" target="_blank">GitHub</a>
            </div>
        </div>
    </section>

    <section id="education">
        <h2 style="font-family: 'Orbitron'; margin-top: 80px;">Education</h2>
        <div class="table-container">
            <table>
                <thead>
                    <tr><th>Degree</th><th>Institution</th><th>Year</th></tr>
                </thead>
                <tbody>
                    <tr><td>Matriculation</td><td>D.B PUBLIC SCHOOL ARA</td><td>2019</td></tr>
                    <tr><td>Intermediate</td><td>S.B College Ara</td><td>2021</td></tr>
                    <tr><td>BCA</td><td>Amity University Patna</td><td>2022-2025</td></tr>
                </tbody>
            </table>
        </div>
    </section>

    <section id="projects">
        <h2 style="font-family: 'Orbitron'; margin-top: 80px;">Featured Projects</h2>
        <div class="project-grid">
            <div class="project-card">
                <b style="color: var(--primary); font-size: 1.2rem;">Adventure Car Racing</b>
                <p>Unity Engine high-octane racing experience with advanced physics.</p>
            </div>
            <div class="project-card">
                <b style="color: var(--primary); font-size: 1.2rem;">Game Server UI</b>
                <p>Sleek dashboard for managing high-performance game servers.</p>
            </div>
            <div class="project-card">
                <b style="color: var(--primary); font-size: 1.2rem;">Speaking Dictionary</b>
                <p>Android app using Java & XML for pronunciation and definitions.</p>
            </div>
        </div>
    </section>

    <section id="contact" style="margin-top: 80px;">
        <h2 style="font-family: 'Orbitron';">Send Your Query</h2>
        <div class="contact-box">
            <input type="text" placeholder="Enter your name">
            <input type="email" placeholder="Enter your email">
            <textarea rows="5" placeholder="How can we collaborate?"></textarea>
            <button class="theme-toggle" style="width: 100%; padding: 15px; margin: 0;">SEND MESSAGE</button>
        </div>
    </section>

    <footer style="text-align: center; padding: 60px 0; opacity: 0.7;">
        <p>© <span id="year"></span> Ritesh Kumar | Built with Passion</p>
    </footer>
</div>

<script>
    document.getElementById('year').textContent = new Date().getFullYear();

    function toggleTheme() {
        const body = document.body;
        body.getAttribute('data-theme') === 'light' ? body.removeAttribute('data-theme') : body.setAttribute('data-theme', 'light');
    }

    function animateSkills() {
        document.querySelectorAll('.skill-per').forEach(el => {
            el.style.width = el.getAttribute('data-width');
        });
    }
</script>

</body>
</html>
